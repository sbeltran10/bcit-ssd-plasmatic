import { StyleSheet } from 'react-native';

export default StyleSheet.create({  
      listContainer:{
        marginTop:30
      },
      items: {
        fontSize: 20,
        textAlign: 'center'
      }
    })